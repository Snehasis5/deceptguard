# DeceptGuard requirements
# Core (required)
anthropic>=0.40.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Optional: AP-H activation probe (requires GPU for practical use)
# torch>=2.0.0
# transformers>=4.40.0
# baukit>=0.1.0

# Testing
pytest>=7.0.0
